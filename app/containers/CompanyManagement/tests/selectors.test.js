// import { selectCompanyManagementDomain } from '../selectors';

describe('selectCompanyManagementDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
