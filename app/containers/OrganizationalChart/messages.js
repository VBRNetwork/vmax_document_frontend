/*
 * OrganizationalChart Messages
 *
 * This contains all the text for the OrganizationalChart container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OrganizationalChart';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrganizationalChart container!',
  },
});
