/*
 * AddContract Messages
 *
 * This contains all the text for the AddContract container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddContract';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddContract container!',
  },
});
