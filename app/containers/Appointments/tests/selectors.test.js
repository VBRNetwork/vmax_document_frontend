// import { selectAppointmentsDomain } from '../selectors';

describe('selectAppointmentsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
