// import { selectPayrollManagementDomain } from '../selectors';

describe('selectPayrollManagementDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
