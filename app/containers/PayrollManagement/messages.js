/*
 * PayrollManagement Messages
 *
 * This contains all the text for the PayrollManagement container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PayrollManagement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PayrollManagement container!',
  },
});
