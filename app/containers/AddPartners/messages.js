/*
 * AddPartners Messages
 *
 * This contains all the text for the AddPartners container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddPartners';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddPartners container!',
  },
});
