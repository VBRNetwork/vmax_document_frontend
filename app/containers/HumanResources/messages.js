/*
 * HumanResources Messages
 *
 * This contains all the text for the HumanResources container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HumanResources';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HumanResources container!',
  },
});
