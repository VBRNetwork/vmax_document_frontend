/*
 * PublicRoute Messages
 *
 * This contains all the text for the PublicRoute component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PublicRoute';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PublicRoute component!',
  },
});
