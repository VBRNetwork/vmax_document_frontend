/*
 * AuthRedirect Messages
 *
 * This contains all the text for the AuthRedirect component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AuthRedirect';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AuthRedirect component!',
  },
});
