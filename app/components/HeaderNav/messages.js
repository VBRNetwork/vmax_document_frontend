/*
 * HeaderNav Messages
 *
 * This contains all the text for the HeaderNav component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HeaderNav';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HeaderNav component!',
  },
});
