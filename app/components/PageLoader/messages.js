/*
 * PageLoader Messages
 *
 * This contains all the text for the PageLoader component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PageLoader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PageLoader component!',
  },
});
